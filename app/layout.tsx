import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Saturn Chess Reader - PGN Analysis & Editor',
  description: 'A comprehensive PGN chess reader and editor with engine analysis, similar to Lichess and Chess.com',
  keywords: 'chess, PGN, analysis, reader, editor, engine, stockfish',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <div className="min-h-screen bg-background">
          {children}
        </div>
      </body>
    </html>
  )
} 